import streamlit as st

def app():
    st.title('Home')

    st.write('This is the `home page` of the Project.')

    st.write('In this Project, we will be using various  ML algorithms and study how diffeirent machine learning algorithms Perform on the our dataset. we have also build a page in the project where one can dynamically build their ML algotithms and know which algorithms work best for their dataset ')
